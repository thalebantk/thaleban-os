# Build output
/build/

# Bootloader — cloned and built by `make limine/limine`
/limine/

# Compilation database + clangd index (regenerate with `make compdb`)
/compile_commands.json
/.cache/

# Stray object/dep files if anything is built outside build/
*.o
*.d
*.iso

# Editor / OS cruft
*.swp
*~
.DS_Store
