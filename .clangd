# clangd reads compile_commands.json (generated by `make compdb`) but doesn't
# understand a couple of GCC-only flags this kernel builds with.
CompileFlags:
  Remove:
    - -mno-80387          # GCC spelling; clang calls it -mno-x87
    - -fno-stack-check
  Add:
    - -mno-x87
    - -ffreestanding
